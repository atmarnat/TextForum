
<h1>ListUsers</h1>


<table style="text-align: left">
    <tr>
        <th>User Name</th>
        <th>Email</th>
        <th>Action</th>
    </tr>
    @foreach (var user in Model)
    {
        <tr>
            <th>@user.UserName</th>
            <th>@user.Email</th>
            <th>
                <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post">
                    <a asp-ation="EditUser" asp-controller="Admin" asp-route-id="@user.Id" class="btn btn-primary">Edit</a>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </th>
        </tr>
    }
</table>
